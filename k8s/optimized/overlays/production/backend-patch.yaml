apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: reconciliation-platform
spec:
  template:
    spec:
      containers:
      - name: backend
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "2000m"
