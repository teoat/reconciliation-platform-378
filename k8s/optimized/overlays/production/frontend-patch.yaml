apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: reconciliation-platform
spec:
  template:
    spec:
      containers:
      - name: frontend
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
