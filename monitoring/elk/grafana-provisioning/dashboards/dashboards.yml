apiVersion: 1

providers:
  - name: 'Reconciliation Platform Dashboards'
    orgId: 1
    folder: 'Reconciliation Platform'
    folderUid: 'reconciliation'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
