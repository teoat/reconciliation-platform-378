FROM rust:1.90-alpine
RUN apk add --no-cache musl-dev pkgconfig openssl-dev postgresql-dev
WORKDIR /app
COPY backend/ .
RUN cargo build --release
CMD ["cp", "target/release/reconciliation-backend", "/output/"]